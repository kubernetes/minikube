name: "update-crun-version"
on:
  workflow_dispatch:
  schedule:
    # every Wednesday at around 3 am pacific/10 am UTC
    - cron: "0 10 * * 3"
permissions:
  contents: read

jobs:
  bump-crun-version:
    uses: ./.github/workflows/update-dependency.yml
    with:
      dep: crun
      title: "Kicbase/ISO: Update crun from {OLD_VERSION} to {NEW_VERSION}"
      branch-suffix: short-commit-hash
      release-url: https://github.com/containers/crun/releases
      ok-to-build-image: true
      ok-to-build-iso: true
      workflow-file: update-crun-version.yml
    secrets: inherit
