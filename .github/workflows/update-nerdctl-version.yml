name: "update-nerdctl-version"
on:
  workflow_dispatch:
  schedule:
    # every Saturday at 1:00 Pacific/8:00 UTC
    - cron: "0 8 * * 6"
permissions:
  contents: read

jobs:
  bump-nerdctl-version:
    uses: ./.github/workflows/update-dependency.yml
    with:
      dep: nerdctl
      title: "Kicbase/ISO: Update nerdctl from {OLD_VERSION} to {NEW_VERSION}"
      branch-suffix: short-commit-hash
      release-url: https://github.com/containerd/nerdctl/releases
      ok-to-build-image: true
      ok-to-build-iso: true
      workflow-file: update-nerdctl-version.yml
    secrets: inherit
