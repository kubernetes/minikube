apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik-test
spec:
  ingressClassName: traefik
  rules:
  - host: traefik-nginx.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: traefik-nginx
            port:
              number: 80

