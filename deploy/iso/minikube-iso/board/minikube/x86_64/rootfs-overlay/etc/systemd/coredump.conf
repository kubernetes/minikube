# Log crash metadata and stack traces to the journal but do not store core files.
# View crash reports with: coredumpctl list / coredumpctl info
#
# WARNING: To store full core files, set Storage=external and increase cluster memory
# to avoid out of memory — core files are stored in tmpfs (/var/lib/systemd/coredump).
[Coredump]
Storage=none
